// Local typed storage uses the same AML compiler and VM.
program LocalStore {
  state {
    item: string
  }

  public fn write(text: string): string {
    self.item = text
    return self.item
  }
}