program Forms {
  form add [many base: int] (once arg: int) ->[many] int marks {}
    under {steps[3], depth[0], work[3]} = base + arg
  term
    use add[10](1) as once first: int in
    use add[10](2) as once second: int in
    first + second
}